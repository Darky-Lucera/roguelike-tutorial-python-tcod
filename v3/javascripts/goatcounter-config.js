// Must load before count.js: sets the endpoint it reads instead of the
// data-goatcounter attribute, which MkDocs' extra_javascript can't add.
window.goatcounter = { endpoint: 'https://caragones.goatcounter.com/count' };
